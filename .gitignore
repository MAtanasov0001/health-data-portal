# --- Тайни: НУЛА тайни в хранилището (публично от ден 1). Сканирането е блокираща CI порта. ---
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
secrets/
**/secrets/
*.secret
**/ckan_api_token

# Node / Next.js
node_modules/
.next/
next-env.d.ts
out/
dist/
build/
coverage/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Данни/снапшоти (не се комитват — генерират се от тръбата)
/data/
**/snapshots/
*.snapshot

# Реални титулярски експорти и производният ПРЕДИ-потискане source.csv: НЕ се комитват в
# публичното репо — съдържат малките клетки, които контролът на разкриването потиска.
# Комитват се само spec-файловете (schema.yaml, metadata.yaml). Виж pilot/README.md.
pilot/deynosti-kp-nzok-2025/source.csv
pilot/deynosti-lekarstva-nzok-2025/source.csv

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars
!*.tfvars.example

# Docker
docker-compose.override.yml

# Agent/workspace tooling (OpenKBS staging workspace — not portal deliverables)
.claude/
.uploads/

# OpenKBS демо/staging: статичен експорт на /frontend, деплойван за жив визуален преглед.
# Изхвърляем билд-артефакт — каноничният frontend е в /frontend, не тук.
/site/
