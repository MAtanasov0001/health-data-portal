# Портал за отворени данни в здравеопазването

**Open Health Data Portal (Bulgaria)** — публичен, машинночетим достъп до анонимизирани и
агрегирани здравни данни, предоставяни от техните титуляри (Министерство на здравеопазването,
НЗОК, „Информационно обслужване“ АД и др.).

Инициатива на **Министерството на иновациите и дигиталната трансформация (МИДТ)**, изпълнявана
като **отворена разработка** съвместно с ИТ общността в България. Продължение на проекта
*Диагноза България*.

> Каноничен архитектурен документ: `docs/concept-v2.0.md` (концепция v2.0, приведена в
> съответствие с **НОИИСРЕАУ**). Този код реализира стартовата (pre-Phase-0) основа.

---

## Статус

🟡 **Pre-Phase-0 основа.** Изграден е вертикален срез от **режим A/B** (вж. `docs/review-model.md`):
слой за приемане/ETL, публично четящо API и метаданен профил.

Пластът на сигурността (**режим C**) — защитни хедъри/rate limit, доставчик на тайни, идентичност
и оторизация, SSRF-устойчив клиент, еднопосочна псевдонимизация (чл. 15, ал. 4) — вече е **създаден
предварително** по решение на титуляра (2026-07-23). Целият режим-C код е **изключен/неактивен по
подразбиране** и подлежи на експертен одит + тест за проникване преди активиране. Инвентар, модел на
заплахите и контролен списък за активиране: `docs/security-architecture.md`.

## Архитектура (накратко)

Хибриден модел на данните: **приемане и кеширане по подразбиране** (harvest + версиониран
снапшот), **жив достъп само по изключение** през строго контролиран интеграционен слой.
Публичните потребители четат от локалното копие, никога пряко от системите на титулярите.

```
Титуляри (API/файл) ──▶ [Интеграционен слой] ──▶ [Приемане/ETL + контрол на разкриването]
                                                          │
                                          версиониран снапшот (immutable)
                                                          ▼
                            [Каталожно ядро: CKAN + DCAT-AP]  ◀──▶  [Публично API]
                                                          ▲                 ▲
                                                          └──── [Frontend (Next.js)] ──── потребители
```

## Структура на монорепото

| Папка | Съдържание | Режим на ревю |
|-------|-----------|---------------|
| `frontend/` | Публичен уебпортал (Next.js, React, TS) — 5 страници + шаблон за набор | A |
| `api/` | Публично четящо API (OpenAPI 3.1, DCAT/RDF, CKAN-съвместимо) | B |
| `api/api/security/` | Пласт за сигурност: хедъри, CORS, rate limit, тайни, SSRF-клиент | **C** |
| `api/api/auth/` | Идентичност и оторизация (pluggable IdP, route guard) | **C** |
| `ingestion/` | Приемане/ETL (Python, FastAPI, Pydantic): схема, контрол на разкриването, версии | B |
| `ingestion/ingestion/anonymization/` | Еднопосочна псевдонимизация (чл. 15, ал. 4) | **C** |
| `catalog/` | CKAN каталожно ядро (вариант А) — конфигурация и docker-compose | B |
| `spec/dcat-ap/` | DCAT-AP 3.0.1 профил + контролирани речници | B |
| `infra/` | Инфраструктура като код (Terraform/Ansible), локален docker-compose | B |
| `pilot/` | Пилотен файлов набор (Диагноза България) + метаданни | A |
| `docs/` | Архитектура, модел на ревю, сигурност, нормативно съответствие | — |

## Технологичен стек

Next.js (React, TypeScript) · CKAN · Python (FastAPI + Pydantic) · PostgreSQL · Redis ·
S3-съвместимо обектно хранилище · Reverse proxy + WAF · Docker + IaC. Изборът е обоснован в
`docs/concept-v2.0.md`, раздел 16.

## Локална разработка

```bash
# Цялостна локална среда (CKAN + API + frontend + Postgres + Redis)
docker compose -f infra/docker-compose.yml up

# Само frontend
cd frontend && npm install && npm run dev

# Само тръбата за приемане (пилотен набор)
cd ingestion && pip install -e . && python -m ingestion.run --dataset ../pilot/diagnoza-bg
```

## Сигурност

Пластът на сигурността е secure-by-default и fail-closed. Активни контроли на публичното API:
защитни HTTP хедъри (CSP, `nosniff`, `X-Frame-Options`), CORS по избор, ограничаване на честотата,
лимит за размер на тялото, безопасно обработване на грешки (без изтичане) и канонична валидация на
идентификаторите. Готови, но **изключени по подразбиране** seam-ове: доставчик на тайни, идентичност
+ MFA (route guard), SSRF-устойчив клиент за бъдещ харвест/жив proxy и еднопосочна псевдонимизация.

Пълен инвентар, модел на заплахите, конфигурация през околната среда и контролен списък за
активиране: **`docs/security-architecture.md`**. Управление на ревюто: `docs/review-model.md`.
Разкриване на уязвимости: `SECURITY.md` (не в публични задачи).

## Принос

Проектът се разработва открито. Външни лица могат да предлагат подобрения по реда на **чл. 60,
ал. 2–4 НОИИСРЕАУ** — възложителят ги разглежда в **30-дневен срок**. Вж. `CONTRIBUTING.md`.
Уязвимости — по реда на `SECURITY.md` (не в публични задачи).

## Лиценз

MIT (работна стойност — вж. `LICENSE`). Окончателният лиценз (MIT или EUPL-1.2) се фиксира на
Фаза 0 след правно становище, преди първия публичен комит с външни приноси.

## Нормативна рамка

Разработката следва **НОИИСРЕАУ** (глава пета в пълен обем), Закона за електронното управление,
Директива (ЕС) 2019/1024, EHDS, ОРЗД, DCAT-AP 3.0.1 и EN 301 549. Порталът **не е регистър** и
**не предоставя ЕАУ** — анализът на пропорционалност е в `docs/concept-v2.0.md`, раздел 5.
