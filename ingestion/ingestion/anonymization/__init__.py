"""Псевдонимизация/анонимизация на идентификатори (чл. 15, ал. 4).

Еднопосочна функция + сол: преобразува преки идентификатори в псевдоними, които не могат
да се обърнат обратно без тайната (pepper). Детерминистично в рамките на един контекст (за да
се агрегира/свързва вътре в набора), но несвързваемо между контексти (различна сол → различни
псевдоними за същия субект в различни набори).

**Изключено по подразбиране.** Титулярите подават вече агрегирани/анонимизирани данни, затова
тръбата не прилага този модул, освен ако наборът изрично не декларира :class:`PseudonymSpec`.
Модулът е готов seam за момента, в който на портала постъпят данни на запис-ниво.

Режим C (docs/review-model.md): критичен компонент — подлежи на одит по сигурност.
"""

from __future__ import annotations

from .pseudonymize import (
    AnonymizationError,
    Pseudonymizer,
    PseudonymSpec,
    pseudonymize_rows,
)

__all__ = [
    "AnonymizationError",
    "PseudonymSpec",
    "Pseudonymizer",
    "pseudonymize_rows",
]
